import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
await import('./lib/ec-runtime-successor-v147-r4-context.mjs');
const read = (file) => fs.readFileSync(new URL(`../${file}`, import.meta.url));
const manifest = JSON.parse(read('docs/freeze/ec-v116-canonical-polling-v147-r4-20260910.json'));
const sha = (bytes) => crypto.createHash('sha256').update(bytes).digest('hex');
for (const [file, expected] of Object.entries(manifest.preservedFiles)) assert.equal(sha(read(file)), expected, `preservado ${file}`);
assert.deepEqual(manifest.functionalFiles, ['scripts/post-sale-transactional-batch-v116.mjs', 'src/services/shipmentStatusDispatcherService.js']);
const poll = read('src/services/shipmentStatusDispatcherService.js').toString();
const batch = read('scripts/post-sale-transactional-batch-v116.mjs').toString();
assert.match(poll, /SHIPMENT_CARRIER_STATUS_SWEEP_INTERVAL_MINUTES', 60/);
assert.match(poll, /raw\.carrierTracking\.lastCheckedAt/);
assert.match(poll, /automation\.dispatchLockedUntil/);
assert.match(poll, /review\.suppressedNotificationKinds/);
assert.match(poll, /assertPostSaleTransactionalV105Configuration\(process\.env\)/);
assert.match(poll, /transactionalV116 && !\['ValidationError', 'CastError'\]\.includes\(error\.name\)\) throw error/);
assert.match(batch, /\.activation-complete\.json/);
assert.ok(batch.indexOf('await processCarrierStatusSweep(') < batch.indexOf('await processShipmentStatusDispatch('));
assert.doesNotMatch(batch, /setInterval|setTimeout|startScheduler|schedulerService/);
assert.equal(manifest.policy.realMessagesSent, 0);
assert.equal(manifest.policy.transport, 'SINK');
assert.equal(manifest.policy.newScheduler, false);
assert.equal(manifest.policy.newPoller, false);
await import('./guard-v146-definitive-normalization.mjs');
console.log('V147_R4_V116_CANONICAL_POLLING_GUARD=PASS');
